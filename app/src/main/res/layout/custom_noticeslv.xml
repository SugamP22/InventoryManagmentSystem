<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    android:background="@drawable/shape_notification_card"
    android:orientation="horizontal"
    android:padding="0dp"
    android:elevation="2dp"
    android:minHeight="56dp">

    <!-- Accent bar: low-stock indicator -->
    <View
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:background="@drawable/notification_accent_bar" />

    <TextView
        android:id="@+id/customTV"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:lineSpacingExtra="2dp"
        android:textColor="@color/notification_text_primary"
        android:textSize="15sp"
        android:textStyle="bold" />
</LinearLayout>
