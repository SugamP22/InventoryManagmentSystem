package com.example.inventorymanagmentsystem.models;

/**
 * I use this enum to distinguish stock movements: ENTRY when we add quantity, EXIT when we remove it.
 */
public enum TransactionType {
    ENTRY, EXIT
}
